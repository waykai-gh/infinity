# Production Deployment Checklist — Infinity

> Используйте этот чеклист перед каждым развёртыванием в production

---

## 📋 Pre-Deployment (перед запуском)

### Подготовка кода

- [ ] Все изменения закоммичены и отправлены в main ветку
- [ ] `git log` показывает последний коммит с вашим сообщением
- [ ] Нет локальных изменений: `git status` чистый
- [ ] `.env` файлы **НЕ** закоммичены (проверить `.gitignore`)
- [ ] Все зависимости установлены: `npm install` во всех папках
- [ ] TypeScript компилируется без ошибок: `npm run build`
- [ ] Тесты пройдены (если есть): `npm test`

### Проверка конфигурации

- [ ] `.env` файл содержит все необходимые переменные:
  - `BOT_TOKEN` установлен
  - `BOT_XOR_TOKEN` установлен
  - `XRAY_ADMIN_TOKEN` установлен
  - `AI_BASE_URL` корректный
  - `OLLAMA_BASE_URL` корректный
  - `OLLAMA_MODEL` доступна

- [ ] SSH доступ на сервер работает
- [ ] Достаточно места на диске на сервере (`df -h`)
- [ ] Docker и Docker Compose установлены на сервере
- [ ] Git репозиторий доступен с сервера (`git clone` работает)

### Безопасность

- [ ] Все токены и ключи в `.env`, не в коде
- [ ] `XRAY_ADMIN_TOKEN` сложный (более 16 символов)
- [ ] SSH ключи настроены (publickey auth)
- [ ] Пароли не хранятся в переменных окружения (только токены)
- [ ] Нет debug логирования в production коде
- [ ] CORS правильно настроен (если есть)

---

## 🚀 During Deployment (во время развёртывания)

### На локальной машине

```bash
# 1. Финальная проверка кода
git status                          # должно быть чистым
git log -1                          # должно быть ваше изменение

# 2. Сборка проекта локально (опционально)
docker-compose build

# 3. Отправка на GitHub
git push origin main
```

### На сервере (SSH подключение)

```bash
# 1. Подключиться на сервер
ssh user@ip

# 2. Перейти в папку проекта
cd infinity

# 3. Проверить текущий статус
docker-compose ps

# 4. Обновить код с GitHub
git pull origin main

# 5. Проверить что .env файл есть и актуален
ls -la .env
cat .env | head -5  # показать первые 5 строк

# 6. Остановить старые контейнеры
docker-compose down

# 7. Очистить неиспользуемые образы (опционально)
docker image prune -f

# 8. Запустить с пересборкой
docker-compose up -d --build
```

### Проверка запуска

```bash
# Дождитесь 10-15 секунд и проверьте статус
docker-compose ps

# ✅ Статусы должны быть:
# STATUS: Up (или Up ... (healthy) если есть healthcheck)
# ❌ НЕ должно быть: Exited, Restarting

# Проверьте логи на ошибки
docker-compose logs | head -50

# Если что-то не так, смотрите последние логи конкретного сервиса
docker-compose logs -f bot          # вынимает логи бота в реальном времени
```

---

## ✅ Post-Deployment (после развёртывания)

### Функциональное тестирование

#### Telegram боты

- [ ] Основной бот (@...main) отвечает на `/start`
- [ ] XOR бот (@infinityXorAi_bot) отвечает на `/start`
- [ ] XOR бот отвечает на простой текст (тестируем AI)
- [ ] Кнопки работают в основном боте
- [ ] Бот не падает при частых сообщениях

#### HTTP API

```bash
# Выполнить на машине, откуда запущен бот @NNfinity

#В cmd объясвить переменные:
TOKEN="your_xray_admin_token"
API="http://127.0.0.1:3001"

# 1. Получить список пользователей (должно быть пусто или несколько)
curl -H "X-Admin-Token: $TOKEN" $API/api/xray/users

# 2. Добавить тестового пользователя
curl -X POST $API/api/xray/users \
  -H "X-Admin-Token: $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"uuid":"test-00000000-0000-0000-0000-000000000001","email":"test@example.com"}'

# 3. Получить статистику (должна быть 0, 0)
curl -H "X-Admin-Token: $TOKEN" $API/api/xray/users/test@example.com/stats

# 4. Удалить тестового пользователя
curl -X DELETE -H "X-Admin-Token: $TOKEN" $API/api/xray/users/test@example.com

# 5. Проверить что удален
curl -H "X-Admin-Token: $TOKEN" $API/api/xray/users
```

- [ ] Все HTTP запросы возвращают статус 200 или ожидаемую ошибку
- [ ] Нет 500 ошибок (Internal Server Error)

#### AI сервис

```bash
# Проверить здоровье Ollama
curl http://62.181.44.4:3000/health/ollama

# Должен вернуть: { "ok": true, "models": [...] }
```

- [ ] AI API доступен
- [ ] Ollama модели загружены
- [ ] XOR бот получает ответы от AI (может быть медленно первый раз)

### Производительность и логи

- [ ] Нет ошибок в логах: `docker-compose logs | grep -i error`
- [ ] Память не растёт бесконечно: `docker stats`
- [ ] CPU использование нормальное: `docker stats`
- [ ] Диск не переполнен: `df -h`
- [ ] Нет логов размером гигабайты (`du -sh /path/to/logs`)

### Мониторинг на следующие часы

- [ ] Боты продолжают отвечать
- [ ] Нет ошибок связи между сервисами
- [ ] Нет неожиданных перезагрузок контейнеров
- [ ] API продолжает работать

---

## 🚨 Откат (если что-то пошло не так)

### Быстрый откат

```bash
# На сервере
cd infinity

# Вернуться к предыдущему коммиту
git reset --hard HEAD~1

# Пересобрать с старым кодом
docker-compose up -d --build

# Проверить статус
docker-compose ps
```

### Полный откат (если база данных повреждена)

```bash
# Остановить всё
docker-compose down -v  # -v удалит все тома!

# Стереть все образы (опционально)
docker image prune -a -f

# Начать заново
docker-compose up -d --build
```

⚠️ **ВНИМАНИЕ:** `docker-compose down -v` удаляет ВСЕ данные! Используйте только если подготовили backup.

---

## 📊 Что мониторить после деплоя

### Каждый час (первые 6 часов)

```bash
# Проверить логи
docker-compose logs --tail=100

# Проверить статус контейнеров
docker-compose ps

# Проверить что боты отвечают
# (отправить тестовое сообщение в Telegram)
```

### Ежедневно

```bash
# Общая статистика
docker stats --no-stream

# Использование диска
df -h

# Проверить нет ли перезагрузок
docker-compose ps
```

### Еженедельно

```bash
# Очистить старые логи (если они растут)
docker-compose logs --timestamps | tail -1000 > logs_backup.txt
docker container prune -f  # удалить стопанные контейнеры

# Проверить обновления зависимостей
npm outdated
```

---

## 🔧 Частые проблемы при деплое

| Проблема | Симптомы | Решение |
|----------|----------|----------|
| **Port already in use** | `EADDRINUSE: address already in use` | `docker-compose down` перед запуском |
| **Out of memory** | Контейнеры постоянно перезагружаются | `docker container prune -f && docker image prune -f` |
| **Git pull fails** | `fatal: not a git repository` | Убедиться что в правильной папке |
| **Docker не запускается** | `Cannot connect to Docker daemon` | `sudo service docker start` или перезагрузка |
| **API 503 Service Unavailable** | Ollama не отвечает | Дождитесь загрузки модели или проверьте OLLAMA_BASE_URL |
| **Бот не отвечает** | Долгое молчание бота | Проверить BOT_TOKEN и интернет соединение |

---

## 📈 Что делать если выросла нагрузка

### Краткосрочное решение

1. Увеличить лимиты Docker контейнеров
2. Добавить кеширование (Redis)
3. Оптимизировать запросы к Xray API

### Долгосрочное решение

1. Миграция на Kubernetes
2. Load balancing между несколькими серверами
3. PostgreSQL для персистентного хранилища
4. Отдельные серверы для Ollama
5. CDN для статического контента

---

## 📝 Документация и логирование

### Что документировать при каждом деплое

```markdown
## Deployment на YYYY-MM-DD

### Что было изменено
- Описание изменений
- Файлы которые изменились

### Результат
- ✅ API работает
- ✅ Боты отвечают
- ❌ Какая-то проблема (опционально)

### Команды
```
git commit ...
docker-compose up ...
```

### Контакт
@username в случае проблем
```

### Где хранить документ
```bash
# Создать файл с датой
echo "## Deployment на $(date +%Y-%m-%d)" > deployments/$(date +%Y-%m-%d).md

# Добавить в Git
git add deployments/
git commit -m "docs: deployment log for $(date +%Y-%m-%d)"
git push
```

---

## ✨ Итоговый чеклист

Перед тем как сказать "готово", убедитесь:

- [ ] Код на GitHub в main ветке
- [ ] .env файлы на сервере (не в Git)
- [ ] Все контейнеры запущены (`docker-compose ps` = Up)
- [ ] Логи чистые (нет красных ошибок)
- [ ] Telegram боты отвечают
- [ ] HTTP API работает
- [ ] Нет 500 ошибок
- [ ] Документация обновлена
- [ ] Deployment залогирован

**Если все пункты галочкой — поздравляем! Развёртывание успешно! 🎉**

---

## 📞 Когда звать на помощь

Обратитесь к документации если:
- Сервис не запускается → INFRASTRUCTURE.md → Диагностика
- Ошибка в коде → посмотрите логи в `docker-compose logs`
- Нужно добавить новый компонент → INFRASTRUCTURE.md → Компоненты
- Непонятно как что-то работает → QUICK_REFERENCE.md

Если всё стало критично:
1. Выполните [откат](#откат-если-что-то-пошло-не-так)
2. Изучите логи
3. Обратитесь к документации
4. Если помощь нужна срочно — напишите в чат команды

---

**Версия:** 1.0  
**Дата:** 3 января 2026  
**Для каких версий:** v2.0 и выше
